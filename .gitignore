dist/
node_modules/
.npmrc
coverage/
*.log
.DS_Store

# Build artifacts
dist.tar.gz
*.tgz

# Examples env files — never commit real credentials
examples/**/.env

# Claude Code (personal, not project-specific)
.claude/

# Oh-My-Claudecode (session and state)
.omc/

# pnpm lockfile (npm is the primary package manager)
pnpm-lock.yaml
